const friends = ['abul', 'labil', 'munna', 'nahid'];
const new1 = `<p class="friend">Friends-${friends.length}</p>`;
console.log(new1);

const first = 'Munna';
const last = 'Hossain';
const fullName = `${first} ${last}`;
console.log(fullName);
